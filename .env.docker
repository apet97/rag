# RAG System Docker Environment Configuration
# Copy this to .env and customize for your deployment

# ===== REQUIRED =====
# IMPORTANT: Change this to a strong random string in production!
API_TOKEN=change-me

# ===== SERVER CONFIGURATION =====
# Development (dev) or Production (prod)
ENV=dev

# Logging level
LOG_LEVEL=INFO

# ===== OLLAMA CONFIGURATION =====
# Address of Ollama server
# For docker-compose: http://ollama:11434
# For host machine: http://host.docker.internal:11434 (macOS/Windows)
# For remote: http://192.168.1.100:11434
LLM_BASE_URL=http://ollama:11434

# Embedding model (must be available in Ollama)
EMBEDDING_MODEL=nomic-embed-text:latest

# Chat model (must be available in Ollama)
LLM_MODEL=gpt-oss:20b

# ===== CACHE CONFIGURATION =====
# Maximum number of responses to cache
RESPONSE_CACHE_SIZE=1000

# Cache time-to-live in seconds
RESPONSE_CACHE_TTL=3600

# ===== RATE LIMITING =====
# Requests per second limit per IP
RATE_LIMIT_RPS=10

# ===== NAMESPACES =====
# Comma-separated list of available namespaces
NAMESPACES=clockify-help
